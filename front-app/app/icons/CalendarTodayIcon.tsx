import React from "react";
import CalendarToday from "react-native-vector-icons/MaterialIcons";

export const CalendarTodayIcon = (props: any) => <CalendarToday {...props} name="calendar-today" />;